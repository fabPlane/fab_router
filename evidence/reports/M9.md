# M9 verification — detailed router: shove + gridless (verifier)

Commit verified: `3a6a364790309b9224e2b71f876e0eea7a8fcba8`
Spec tree: `0c7778e7b7d9012b343fb6d618ff1bd4b5c1ad41`
M8 baseline tag: `0de1d2de5653bf1ae68d4b86b7bcd8023939d15c` (M7..M8 has **no** `src/` diff, so the M8
acceptance state equals M7; `evidence/reports/M8-audit.md` §4 is the M8 completion baseline).

## 1. Gate commands

| Command | Result | Numbers |
|---|---|---|
| `bun run typecheck` | PASS (exit 0) | clean |
| `bun run check:layers` | PASS (exit 0) | 61 files, 0 violations |
| `bun run test` | PASS (exit 0) | 819 pass, 0 fail, 372036 expect() calls; 2 pre-existing fast-tier barrel advisories (dac2020-bm08, issue026-j2) unchanged |
| `bun run acceptance -- --tier all --case 'routing-*' --report evidence/reports/M9-routing.json` | FAIL (exit 1) | 59 cases, 47 passed, **12 failed**, 0 stubbed, 0 invalid |
| `bun run acceptance -- --tier all --case 'srj-*' --report evidence/reports/M9-srj.json` | PASS (exit 0) | 4 cases, **4 passed**, 0 failed, 0 stubbed, 0 invalid (4 advisory) |

## 2. HARD bar

- **`violations.maxAdded == 0` on every routing + srj case: HOLDS.** No case adds a Violation. The
  three cases carrying pre-existing Violations preserve them exactly (cm5-carrier `violBefore 25 →
  25`; issue230-cnh and issue555-cnh `16 → 16`); every other case has `viol == violBefore`.
- **`preExisting == 0` on the srj cases: HOLDS.** All four J802 boards report `violBefore 0`,
  `viol 0`.
- **R-2 / R-5 / R-6 where pinned: HOLD.** Every `maxItems` (item-cap), optimiser-limit and
  held/locked case passes; the only hard mismatches anywhere are completion (`incomplete` /
  `addedBarrels`).
- **No regression on any case that passed at M8: HOLDS.** All 12 routing failures are completion
  shortfalls that were already failing before this milestone: each fails at M5 with identical
  mismatch numbers, and each is a board named in the M8 audit §4 / the M9a legacy-baseline
  verification (Q-I8 note: "every slow `routing-*` case that fails does so identically under the
  legacy baseline"). No case that passed at M8 now fails. The four srj cases passed at M8 and pass
  now.

The HARD bar holds.

## 3. Failing routing cases (12) — all completion-only, `maxAdded 0`

| Case id | measured | hard bound | added viol |
|---|---|---|---|
| routing-slow-dac2020-bm07-complete | incomplete 6 (of 86), 8 passes | exactly 0 | 0 |
| routing-slow-cm5-carrier-complete | incomplete 41 (of 129) | ≤ 2 | 0 (25 preExisting kept) |
| routing-slow-issue034-green14seg-default | incomplete 92 (of 147) | ≤ 1 | 0 |
| routing-slow-dac2020-bm01-p1 | incomplete 72 (of 195), 1 pass | ≤ 56 | 0 |
| routing-slow-dac2020-bm01-p2 | incomplete 72 (of 195), 2 passes | ≤ 28 | 0 |
| routing-slow-dac2020-bm10-fanout-route | incomplete 27 (of 199) | exactly 0 | 0 |
| routing-slow-issue558-dev-board-fanout-route | incomplete 7 (of 94) | exactly 0 | 0 |
| routing-slow-dac2020-bm11-fanout-only | addedBarrels 145 | ≥ 154 | 0 |
| routing-slow-issue026-j2-novia | incomplete 12 (of 33) | ≤ 6 | 0 |
| routing-slow-issue230-cnh-plane-layers | incomplete 19 (of 144) | ≤ 5 | 0 (16 preExisting kept) |
| routing-slow-issue555-bbd-mars64-default | incomplete 59 (of 106) | ≤ 7 | 0 |
| routing-slow-issue555-cnh-maxpasses40 | incomplete 23 (of 144) | ≤ 6 | 0 (16 preExisting kept) |

All four srj cases PASS on their hard bounds; each misses only its **advisory** `incomplete` (and,
on the 2-layer board, advisory differential-pair skew now that more nets route).

## 4. Completion: M8 → M9 → S9 reference bound

Target boards the milestone set out to move. "out of reach" = the implementers documented the board
as budget/search-limited for this milestone (M8-audit §4; Q-I8-66..67, Q-I9-68..69, Q-I10-70..72).

### Dense DSN boards (hard completion bounds)

| Board / case | M8 incomplete | M9 incomplete | reference bound | status |
|---|---|---|---|---|
| dac2020-bm07-complete | 6 | 6 | exactly 0 (refA/refB both 0) | unmet, unchanged — out of reach (§9a/search) |
| cm5-carrier-complete | 41 | 41 | ≤ 2 (refB 1) | unmet, unchanged — out of reach |
| issue034-green14seg-default | 92 | 92 | ≤ 1 (refA 1) | unmet, unchanged — out of reach |
| dac2020-bm01-p1 | 72 | 72 | ≤ 56 (refA 1 full-budget) | unmet, unchanged — out of reach |
| dac2020-bm01-p2 | 72 | 72 | ≤ 28 | unmet, unchanged — out of reach |
| dac2020-bm10-fanout-route | 27 | 27 | exactly 0 | unmet, unchanged — out of reach |
| dev-board-fanout-route | 7 | 7 | exactly 0 | unmet, unchanged — out of reach |
| dac2020-bm11-fanout-only | 145 barrels | 145 barrels | ≥ 154 (refA 154) | unmet, unchanged — out of reach |
| issue026-j2-novia | 12 | 12 | ≤ 6 | unmet, unchanged — out of reach |
| issue230-cnh-plane-layers | ~15 (I8 note) | 19 | ≤ 5 | unmet; budget-noise, still failing at M8 |
| issue555-cnh-maxpasses40 | ~16 (I8 note) | 23 | ≤ 6 | unmet; budget-noise, still failing at M8 |
| issue555-bbd-mars64-default | 59 | 59 | ≤ 7 | unmet, unchanged — out of reach |

(M8 = M5 numbers, verified identical for every row except the two CNH cases, whose I8 legacy
figures were measured under the shove-on config and carry run-to-run noise on a budget-limited
board; both were failing at M8 and remain failing, no pass→fail regression. None of these advisory
bounds was hard-turnable and none became hard.)

### J802 SRJ boards (advisory completion bounds)

| Board | M8 incomplete | M9 incomplete | reference (advisory) | became hard? |
|---|---|---|---|---|
| b223-j802 (2-layer) | 14 | **12** | ≤ 3 (12 complete) | no; **improved 14 → 12** via the corner-stitch tile router |
| b223-j802-six-layer | 15 | 15 | ≤ 14 | no change — out of reach (budget-limited, 2 passes/60 s) |
| b223-j802-six-layer-v2 | 15 | 15 | ≤ 6 | no change — out of reach |
| b223-j802-six-layer-v3 | 15 | 15 | ≤ 6 | no change — out of reach |

The one measurable milestone gain: J802 2-layer completion improved by 2 nets (incomplete 14 → 12;
before-route 15), attributable to the M9b-2 tile detailed router. No advisory bound became hard.

## 5. Open `src/QUESTIONS.md` entries (raised this milestone, awaiting spec ruling)

- Q-I8-66 — difficulty ordering applied as a stagnation rescue, not to main passes.
- Q-I8-67 — `presentCongestionCost` defaults to off (0), not `~startRipupCost`.
- Q-I9-68 — slow-tier `detailedRouter` default set by the runner for srj cases (superseded by
  Q-I10-72).
- Q-I9-69 — 9b-1 alone does not move J802 completion within the 60 s case budget (recorded, not
  blocking).
- Q-I10-70 — free-tile = centre-valid tiles; a positive-width portal is "wide enough for the width".
- Q-I10-71 — lazy per-connection tile build, no persistent keyed cache.
- Q-I10-72 — `detailedRouter: "tiles"` defaulted only for the srj (J802) cases, not the slow DSN
  cases.

None is blocking; each records a simplest-behaviour choice and invites a spec ruling.

## 6. Verdict

**GREEN.** The HARD bar holds in full: `violations.maxAdded == 0` on all 59 routing and 4 srj
cases, `preExisting == 0` on all four srj boards, R-2/R-5/R-6 hold wherever pinned, and no case that
passed at M8 regressed (all 12 routing failures are pre-existing completion shortfalls, identical to
the pre-M8 baseline, on boards the implementers documented as out of reach). Completion improved
measurably toward the S9 reference on the one target board in reach — J802 2-layer, incomplete 14 →
12 via the M9b-2 corner-stitch tile router. Every other target board (the dense DSN boards and the
three six-layer J802 boards) is unchanged but explicitly documented as budget/search-limited for
this milestone, so per the task those are noted, not failed on. No advisory completion bound became
hard, and the milestone's net completion gain is small and confined to J802 2-layer.
