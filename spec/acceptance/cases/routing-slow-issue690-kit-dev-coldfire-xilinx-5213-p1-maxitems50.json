{
  "id": "routing-slow-issue690-kit-dev-coldfire-xilinx-5213-p1-maxitems50",
  "kind": "routing",
  "origin": "refA-suite",
  "tier": "slow",
  "board": "Issue690-kit-dev-coldfire-xilinx_5213.dsn",
  "settings": {
    "maxPasses": 1,
    "maxItems": 50,
    "timeoutSeconds": 120,
    "fanout": false
  },
  "expect": {
    "passes": {
      "max": 1
    },
    "violations": {
      "maxAdded": 0
    },
    "wallClockMs": {
      "max": 44000,
      "advisory": true
    },
    "completed": {
      "max": 50
    }
  },
  "note": "Suite A: the run completes without error on a board whose padstack references once broke a reference. Both references count maxItems as routed connections; the spec's maxItems bounds inserted Tracks plus Barrels (settings.md), so the suite's incomplete bound is recorded as advisory and the hard expectations are the item cap (R-5) and no added violations. maxItems caps completed connections (settings.md ruling 2026-09-13)."
}
