{
  "id": "routing-fast-issue690-ecc83-p1-maxitems50",
  "kind": "routing",
  "origin": "refA-suite",
  "tier": "fast",
  "board": "Issue690-ecc83.dsn",
  "settings": {
    "maxPasses": 1,
    "maxItems": 50,
    "timeoutSeconds": 60,
    "fanout": false
  },
  "expect": {
    "addedTracks": {
      "max": 50
    },
    "addedBarrels": {
      "max": 50
    },
    "passes": {
      "max": 1
    },
    "violations": {
      "maxAdded": 0
    },
    "wallClockMs": {
      "max": 10000,
      "advisory": true
    }
  },
  "note": "Suite A: the run completes without error on a board whose padstack references once broke a reference. Both references count maxItems as routed connections; the spec's maxItems bounds inserted Tracks plus Barrels (settings.md), so the suite's incomplete bound is recorded as advisory and the hard expectations are the item cap (R-5) and no added violations."
}
