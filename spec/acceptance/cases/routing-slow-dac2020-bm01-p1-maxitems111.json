{
  "id": "routing-slow-dac2020-bm01-p1-maxitems111",
  "kind": "routing",
  "origin": "refA-suite",
  "tier": "slow",
  "board": "Issue508-DAC2020_bm01.dsn",
  "settings": {
    "maxPasses": 1,
    "maxItems": 111,
    "timeoutSeconds": 90
  },
  "expect": {
    "incomplete": {
      "max": 134
    },
    "violations": {
      "maxAdded": 0
    },
    "wallClockMs": {
      "max": 58000,
      "advisory": true
    },
    "completed": {
      "max": 111
    }
  },
  "note": "Suite A: one pass capped at 111 routed connections leaves at most 134 incomplete. Both references count maxItems as routed connections; the spec's maxItems bounds inserted Tracks plus Barrels (settings.md), so the suite's incomplete bound is recorded as advisory and the hard expectations are the item cap (R-5) and no added violations. The suite's bound (134) is looser than the measured reference (A: 53); the suite's bound is kept. maxItems caps completed connections (settings.md ruling 2026-09-13)."
}
