{
  "id": "routing-fast-issue676-ch32v-p1-maxitems5",
  "kind": "routing",
  "origin": "refA-suite",
  "tier": "fast",
  "board": "Issue676-ch32v-tx118s.dsn",
  "settings": {
    "maxPasses": 1,
    "maxItems": 5,
    "timeoutSeconds": 60
  },
  "expect": {
    "passes": {
      "max": 1
    },
    "violations": {
      "maxAdded": 0
    },
    "wallClockMs": {
      "max": 10000,
      "advisory": true
    },
    "completed": {
      "max": 5
    }
  },
  "note": "Suite A: the effective settings see 2 Sheets for this board before and after routing (a settings case covers the count); the run completes without error. Both references count maxItems as routed connections; the spec's maxItems bounds inserted Tracks plus Barrels (settings.md), so the suite's incomplete bound is recorded as advisory and the hard expectations are the item cap (R-5) and no added violations. maxItems caps completed connections (settings.md ruling 2026-09-13)."
}
