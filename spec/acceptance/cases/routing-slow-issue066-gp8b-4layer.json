{
  "id": "routing-slow-issue066-gp8b-4layer",
  "kind": "routing",
  "origin": "both",
  "tier": "slow",
  "board": "Issue066-Project_GP8B.dsn",
  "settings": {
    "fanout": false,
    "maxPasses": 2,
    "maxItems": 60,
    "timeoutSeconds": 120
  },
  "expect": {
    "passes": {
      "max": 2
    },
    "violations": {
      "maxAdded": 0
    },
    "completed": {
      "max": 60
    }
  },
  "note": "Suite A: this 4-Sheet board routes with at least one Track on an inner Sheet (index 1 or 2); suite B: the Stack has 4 Sheets from the file, never a default of 2. The inner-Sheet usage is an invariant of rules/layers.md the runner may check through layoutStats. Both references count maxItems as routed connections; the spec's maxItems bounds inserted Tracks plus Barrels (settings.md), so the suite's incomplete bound is recorded as advisory and the hard expectations are the item cap (R-5) and no added violations. Under the 'default' profile reference A hit the profile's 60 s budget with 591 incomplete; reference B hit the profile's 60 s budget with 526 incomplete (see the reference file); the case's own budget is what its settings say. maxItems caps completed connections (settings.md ruling 2026-09-13)."
}
