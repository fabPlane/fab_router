{
  "id": "routing-slow-dac2020-bm05-p1-maxitems2",
  "kind": "routing",
  "origin": "refA-suite",
  "tier": "slow",
  "board": "Issue508-DAC2020_bm05.dsn",
  "settings": {
    "maxPasses": 1,
    "maxItems": 2,
    "timeoutSeconds": 30
  },
  "expect": {
    "incomplete": {
      "max": 106
    },
    "violations": {
      "maxAdded": 0
    },
    "wallClockMs": {
      "max": 60000,
      "advisory": true
    },
    "completed": {
      "max": 2
    }
  },
  "note": "Suite A: one pass capped at 2 routed connections leaves at most 106 incomplete. Both references count maxItems as routed connections; the spec's maxItems bounds inserted Tracks plus Barrels (settings.md), so the suite's incomplete bound is recorded as advisory and the hard expectations are the item cap (R-5) and no added violations. The suite's bound (106) is looser than the measured reference (B: 34); the suite's bound is kept. maxItems caps completed connections (settings.md ruling 2026-09-13)."
}
