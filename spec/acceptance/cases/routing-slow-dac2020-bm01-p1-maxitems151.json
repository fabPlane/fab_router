{
  "id": "routing-slow-dac2020-bm01-p1-maxitems151",
  "kind": "routing",
  "origin": "refA-suite",
  "tier": "slow",
  "board": "Issue508-DAC2020_bm01.dsn",
  "settings": {
    "maxPasses": 1,
    "maxItems": 151,
    "timeoutSeconds": 180
  },
  "expect": {
    "addedTracks": {
      "max": 151
    },
    "addedBarrels": {
      "max": 151
    },
    "incomplete": {
      "max": 126,
      "advisory": true
    },
    "violations": {
      "maxAdded": 0
    },
    "wallClockMs": {
      "max": 58000,
      "advisory": true
    }
  },
  "note": "Suite A: one pass capped at 151 routed connections leaves at most 126 incomplete. Both references count maxItems as routed connections; the spec's maxItems bounds inserted Tracks plus Barrels (settings.md), so the suite's incomplete bound is recorded as advisory and the hard expectations are the item cap (R-5) and no added violations. The suite's bound (126) is looser than the measured reference (A: 53); the suite's bound is kept."
}
